# Example — Goal known, path unknown

User:

“我要让最终 AI 镜头严格跟 Blender 白模的机位和人物路径，同时保留写实演员和街景，但我不知道这些素材在生成平台里到底该怎么接。”

Correct mode: `DISCOVER_PATH`.

Reason: the desired result is sufficiently clear; implementation is not.

Good behavior:

1. Pause paid generation (Path Gate).
2. Verify relevant platform workflow if current behavior matters.
3. Explain feasible asset-role routes.
4. Recommend a route.
5. Ask only the preference that still matters, e.g. whether pacing must also match the white model.
6. Build/update Full Spec before execution.
